
Процедура КнопкаВыполнитьНажатие(Кнопка)
	Параметры = Новый Структура;
	Параметры.Вставить("Источник", СсылкаНаОбъект.ПолучитьОбъект());
	Параметры.Вставить("Отказ", ЛОЖЬ);
	Параметры.Вставить("РежимЗаписи", РежимЗаписиДокумента.Проведение);
	Параметры.Вставить("РежимПроведения", РежимПроведенияДокумента.Неоперативный);
	
	ВыполнитьОбработку(Параметры);
КонецПроцедуры

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	ТекПользователь = СокрЛП(ПараметрыСеанса.ТекущийПользователь.Наименование);
	Отказ = ТекПользователь <> "Соловьёв Андрей";
КонецПроцедуры

Процедура ПриОткрытии()
	ЭтаФорма.Заголовок = "Действия перед записью документа. "+ЭтотОбъект.Метаданные().Комментарий;
КонецПроцедуры
